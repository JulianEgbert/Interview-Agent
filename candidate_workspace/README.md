# Candidate Workspace

InterviewRoom creates one folder per coding challenge here when the agent starts.

Edit the generated `solution.py` for the active challenge, then ask the interviewer
to inspect your code or run the tests. Generated challenge folders are ignored by
git so your scratch work stays local.
